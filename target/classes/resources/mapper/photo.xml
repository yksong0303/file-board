<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
	<select id="selectPb" resultType="map" parameterType="map">
		select * from photo_board;
	</select>
	
	<insert id="insertPb" parameterType="int">
		insert into
		photo_board(pb_num,pb_title,pb_content,pb_photo_name,pb_photo_path,credat,cretim)
		values(seq_pbNum.nextval,#{pb_title},#{pb_content},#{pb_photo_name},#{pb_photo_path},TO_CHAR(sysdate,
		'YYYYMMDD'), TO_CHAR(sysdate,H'H24MISS'));
	</insert>
</mapper>